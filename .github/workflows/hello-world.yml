name: PR Created

on:
  pull_request:
    types: [opened]
  workflow_dispatch:

jobs:
  print_message:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "PR created!"