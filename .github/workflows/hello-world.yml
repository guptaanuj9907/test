name: Close new pull requests

on:
  pull_request:
    types: [opened]

jobs:
  build:

    runs-on: ubuntu-latest
  
    steps:
      
      
      - name: Install dependencies
        run: |
          print("anuj")
        shell: python
      
